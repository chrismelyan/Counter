const initialState = {
    minValue: 0,
    maxValue: 0
}
type InitialStateType = typeof initialState

export const counterValuesReducer = (state: InitialStateType, action: any): InitialStateType => {
    return state
}